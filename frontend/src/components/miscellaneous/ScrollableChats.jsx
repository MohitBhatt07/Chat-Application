import React from 'react'

const ScrollableChats = (messages) => {
  return (
    messages && messages.map((currMessage,index) =>(
      <div className='' ></div>
    ) )
  )
}

export default ScrollableChats